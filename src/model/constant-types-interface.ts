export interface IRole {
    id: string,
    name: string,
    value: number,
}

export interface IOrderState {
    id: string,
    name: string,
    color: string,
    canFeedback: boolean,
}

export interface IPaymentMethod {
    id: string,
    name: string
}
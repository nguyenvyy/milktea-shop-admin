image: rambabusaravanan/firebase

default:
  before_script:
    - npm i

stages:
  - build
  - test
  # - install
  # - deploy

build_job:
  stage: build
  script: npm run build

# install_job:
#   stage: install
#   script:
#     - npm i

test_job:
  stage: test
  script: 
    - npm test


